perry *  checking examples ... ERROR
Running examples in 'perry-Ex.R' failed
The error most likely occurred in:

> base::assign(".ptime", proc.time(), pos = "CheckExEnv")
> ### Name: perryPlot
> ### Title: Plot resampling-based prediction error results
> ### Aliases: autoplot.perry autoplot.perrySelect perryPlot
> ###   perryPlot.default perryPlot.perry perryPlot.perrySelect plot.perry
> ###   plot.perrySelect
> ### Keywords: hplot
> 
> ### ** Examples
> 
> data("coleman")
> set.seed(1234)  # set seed for reproducibility
> 
> ## set up folds for cross-validation
> folds <- cvFolds(nrow(coleman), K = 5, R = 10)
> 
> ## compare LS, MM and LTS regression
> 
> # perform cross-validation for an LS regression model
> fitLm <- lm(Y ~ ., data = coleman)
> cvLm <- perry(fitLm, splits = folds, 
+     cost = rtmspe, trim = 0.1)
> 
> # perform cross-validation for an MM regression model
> fitLmrob <- lmrob(Y ~ ., data = coleman, k.max = 500)
> cvLmrob <- perry(fitLmrob, splits = folds, 
+     cost = rtmspe, trim = 0.1)
Warning in lmrob.S(x, y, control = control) :
  find_scale() did not converge in 'maxit.scale' (= 200) iterations
> 
> # perform cross-validation for an LTS regression model
> fitLts <- ltsReg(Y ~ ., data = coleman)
> cvLts <- perry(fitLts, splits = folds, 
+     cost = rtmspe, trim = 0.1)
> 
> # combine results into one object
> cv <- perrySelect(LS = cvLm, MM = cvLmrob, LTS = cvLts)
> cv

5-fold CV results:
  Fit        PE
1  LS 1.6501492
2  MM 0.9872721
3 LTS 1.1407723

Best model:
  PE 
"MM" 
> 
> # plot results for the MM regression model
> plot(cvLmrob, method = "box")
> plot(cvLmrob, method = "density")
Error in list(x = x, y = y, ...) : 'y' is missing
Calls: plot ... perryPlot -> perryPlot.default -> densityPlot -> aes_string
Execution halted